{
  "name": "month-end-checker-web",
  "private": true,
  "engines": {
    "node": "20.x",
    "npm": "10.x"
  },
  "scripts": {
    "dev": "NEXT_TELEMETRY_DISABLED=1 next dev --webpack -p 3011",
    "build": "rm -rf .next && next build",
    "start": "./node_modules/.bin/next start -p $PORT -H 0.0.0.0",
    "build:ensure": "rm -rf .next && (next build && node -e \"process.exit(require(\\\"fs\\\").existsSync(\\\".next/server/pages-manifest.json\\\")?0:1)\" || (echo \"Retrying with --debug...\" && next build --debug))"
  },
  "dependencies": {
    "next": "16.1.0",
    "react": "18.2.0",
    "react-dom": "18.2.0",
    "scheduler": "^0.27.0",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "@types/node": "25.0.3",
    "@types/react": "^18",
    "@types/react-dom": "^18",
    "@types/xlsx": "^0.0.35",
    "autoprefixer": "^10.4.19",
    "postcss": "^8.4.38",
    "tailwindcss": "^3.4.17",
    "typescript": "^5.9.3"
  }
}
